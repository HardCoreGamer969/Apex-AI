# Core dependencies (backend + data processing)
fastf1
pandas
matplotlib
numpy
fastapi
uvicorn[standard]
orjson>=3.11.7
cachetools>=7.0.5
supabase>=2.25.1

# Desktop GUI (not needed on Render/server deployments)
# Install: uv sync  (core) or  uv sync --extra desktop  (with Arcade/PySide6)
# arcade
# pyglet
# pyside6
# questionary
# rich
